# Stage 2 + pipeline
# ติดตั้ง PyTorch แบบมี CUDA แยกก่อน (ดู https://pytorch.org):
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
ultralytics==8.4.126
opencv-python>=4.9
pillow>=10.0
numpy>=1.26

# Prototype UI (app.py)
gradio>=4.44

# รูปประกอบรายงาน (make_figures.py)
matplotlib>=3.8

# เอกสารไดอาแกรม (make_diagrams_doc.py / make_uml_doc.py / make_design_doc.py)
python-docx>=1.1
# รวม docx เป็นไฟล์เดียว (make_combined_doc.py)
docxcompose>=1.4

# torch / torchvision — ติดตั้งตามคอมเมนต์ด้านบน (ไม่ pin ที่นี่เพราะขึ้นกับ CUDA ของเครื่อง)
torch>=2.2
torchvision>=0.17
