# Virtual environment
venv/
.venv/

# Datasets (ไฟล์ใหญ่เกินไปสำหรับ GitHub)
train/
valid/
test/
merged_dataset/
merged_dataset_gray/
rust_dataset/
crack_dataset/

# Model weights และผลการเทรน
runs/
runs_*.log
*.pt

# ผลลัพธ์ที่ generate ได้เอง
pipeline_results/
real_test_results.json
# scratch output ของ evaluate.py / evaluate_stage1.py (ไฟล์ที่เก็บถาวรตั้งชื่ออื่น)
results/*_latest.json
# ไฟล์เก่า — ย้ายไป results/ แล้ว
evaluation_results*.json

# Python cache
__pycache__/
*.pyc

# MS Office lock files
~$*

# Environment variables
.env

# Cross-dataset test images (โหลดใหม่ได้จาก HF — ดู cross_dataset_eval.md)
external_test/*/images/

# ภาพถ่ายจริง / dataset ที่โหลดจากเน็ต + ผลของ active-learning loop (ไฟล์ใหญ่)
downloads/
photos_round*/
dataset_real/

# Stage 0 steel-gate: dataset ที่ประกอบจาก DTD/Imagenette (สร้างใหม่ได้ด้วย make_gate_dataset.py)
dataset_gate/

# เอกสารอ้างอิง/ร่างของผู้จัดทำ (ไม่ commit)
docs/ต้นแบบ/
docs/งานของผม/
